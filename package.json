{
  "name": "reportes-urbanos-deploy",
  "version": "1.0.0",
  "description": "App de Reportes Ciudadanos para Railway",
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
